import { useEffect, useMemo, useState, type FormEvent } from "react";
import type { MeResponse } from "@hooma/contracts";
import { TeamDiscoveryCard, TeamsHero } from "@hooma/ui";
import { useHoomaFrontend } from "../context";
import type { ManagedTeam, PublicTeamSummary, TeamChallengeSummary, TeamGameSummary } from "../api";

type TeamsTab = "discover" | "challenges" | "games";
function friendlyDate(value: string | null): string { if (!value) return "Scheduling TBA"; const date = new Date(value); return Number.isNaN(date.valueOf()) ? "Scheduling TBA" : new Intl.DateTimeFormat(undefined, { dateStyle: "medium", timeStyle: "short" }).format(date); }
function report(reason: unknown): string { return reason instanceof Error ? reason.message : "Unexpected Teams error"; }

export function TeamsPage() {
  const { api, authenticationHref } = useHoomaFrontend();
  const [tab, setTab] = useState<TeamsTab>("discover");
  const [teams, setTeams] = useState<PublicTeamSummary[]>([]);
  const [nextCursor, setNextCursor] = useState<string | null>(null);
  const [managedTeams, setManagedTeams] = useState<ManagedTeam[]>([]);
  const [incoming, setIncoming] = useState<TeamChallengeSummary[]>([]);
  const [outgoing, setOutgoing] = useState<TeamChallengeSummary[]>([]);
  const [games, setGames] = useState<TeamGameSummary[]>([]);
  const [me, setMe] = useState<MeResponse | null>(null);
  const [search, setSearch] = useState(""); const [city, setCity] = useState(""); const [houma, setHouma] = useState("");
  const [name, setName] = useState(""); const [newTeamCity, setNewTeamCity] = useState(""); const [communityId, setCommunityId] = useState("");
  const [badgeUrl, setBadgeUrl] = useState(""); const [bannerUrl, setBannerUrl] = useState("");
  const [loading, setLoading] = useState(true); const [memberLoading, setMemberLoading] = useState(false); const [creating, setCreating] = useState(false);
  const [actingChallengeId, setActingChallengeId] = useState(""); const [error, setError] = useState(""); const [memberError, setMemberError] = useState(""); const [notice, setNotice] = useState("");
  const eligibleCommunities = useMemo(() => me?.communities.filter((community) => community.role === "FOUNDER" || community.role === "COACH") ?? [], [me]);
  const managedIds = useMemo(() => new Set(managedTeams.map((team) => team.id)), [managedTeams]);
  const challenges = useMemo(() => { const byId = new Map<string, TeamChallengeSummary>(); for (const challenge of [...incoming, ...outgoing]) byId.set(challenge.id, challenge); return [...byId.values()]; }, [incoming, outgoing]);
  const pendingIncomingCount = useMemo(() => incoming.filter((challenge) => challenge.status === "PENDING").length, [incoming]);

  useEffect(() => { let active = true; void api.identity.meOptional().then((response) => { if (!active) return; setMe(response); const firstCommunity = response?.communities.find((community) => community.role === "FOUNDER" || community.role === "COACH"); setCommunityId((current) => current || firstCommunity?.id || ""); }).catch((reason) => { if (active) setMemberError(report(reason)); }); return () => { active = false; }; }, [api]);
  useEffect(() => { let active = true; setLoading(true); setError(""); void api.teams.publicList({ search, city, houma, limit: 30 }).then((response) => { if (!active) return; setTeams(response.items); setNextCursor(response.nextCursor); }).catch((reason) => { if (active) setError(report(reason)); }).finally(() => { if (active) setLoading(false); }); return () => { active = false; }; }, [api, search, city, houma]);
  useEffect(() => { if (!me) { setManagedTeams([]); setIncoming([]); setOutgoing([]); setGames([]); return; } void reloadMemberState(); }, [me]);
  async function reloadMemberState() { setMemberLoading(true); setMemberError(""); try { const [managed, incomingRows, outgoingRows, gameRows] = await Promise.all([api.teams.managed(), api.teams.incomingChallenges(), api.teams.outgoingChallenges(), api.teams.games()]); setManagedTeams(managed); setIncoming(incomingRows); setOutgoing(outgoingRows); setGames(gameRows); } catch (reason) { setMemberError(report(reason)); } finally { setMemberLoading(false); } }
  async function loadMore() { if (!nextCursor || loading) return; setLoading(true); setError(""); try { const response = await api.teams.publicList({ search, city, houma, cursor: nextCursor, limit: 30 }); setTeams((current) => [...current, ...response.items]); setNextCursor(response.nextCursor); } catch (reason) { setError(report(reason)); } finally { setLoading(false); } }
  async function createTeam(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!communityId) return; setCreating(true); setError(""); setNotice(""); try { await api.teams.create({ communityId, name, city: newTeamCity.trim() || null, badgeUrl: badgeUrl.trim() || null, bannerUrl: bannerUrl.trim() || null }); setName(""); setNewTeamCity(""); setBadgeUrl(""); setBannerUrl(""); setNotice("Team created."); const response = await api.teams.publicList({ search, city, houma, limit: 30 }); setTeams(response.items); setNextCursor(response.nextCursor); await reloadMemberState(); } catch (reason) { setError(report(reason)); } finally { setCreating(false); } }
  async function respondToChallenge(id: string, action: "accept" | "decline" | "cancel") { setActingChallengeId(id); setMemberError(""); setNotice(""); try { if (action === "accept") await api.teams.acceptChallenge(id); else if (action === "decline") await api.teams.declineChallenge(id); else await api.teams.cancelChallenge(id); setNotice(`Challenge ${action === "accept" ? "accepted" : action === "decline" ? "declined" : "cancelled"}.`); await reloadMemberState(); } catch (reason) { setMemberError(report(reason)); } finally { setActingChallengeId(""); } }
  const signInHref = authenticationHref("/teams");

  return <div className="page teams-page"><TeamsHero /><nav className="teams-tabs" aria-label="Teams sections"><button className={tab === "discover" ? "teams-tab active" : "teams-tab"} type="button" onClick={() => setTab("discover")}>Discover</button>{managedTeams.length ? <><button className={tab === "challenges" ? "teams-tab active" : "teams-tab"} type="button" onClick={() => setTab("challenges")}>Challenges {pendingIncomingCount ? <b>{pendingIncomingCount}</b> : null}</button><button className={tab === "games" ? "teams-tab active" : "teams-tab"} type="button" onClick={() => setTab("games")}>Games</button></> : null}</nav>{notice ? <div className="success-box">{notice}</div> : null}{error ? <div className="error-box">{error}</div> : null}{tab === "discover" ? <><section className="teams-filter-bar panel"><label className="field"><span>Search</span><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Team, city or houma" /></label><label className="field"><span>City</span><input value={city} onChange={(event) => setCity(event.target.value)} placeholder="Any city" /></label><label className="field"><span>Houma</span><input value={houma} onChange={(event) => setHouma(event.target.value)} placeholder="Any houma" /></label></section>{me ? eligibleCommunities.length ? <form className="inline-form panel team-create-panel" onSubmit={createTeam}><div><span className="eyebrow">CREATE TEAM</span><h2>Start your side</h2></div><label className="field"><span>Community</span><select value={communityId} onChange={(event) => setCommunityId(event.target.value)} required>{eligibleCommunities.map((community) => <option key={community.id} value={community.id}>{community.name}</option>)}</select></label><label className="field"><span>Team name</span><input value={name} onChange={(event) => setName(event.target.value)} placeholder="Team name" required /></label><label className="field"><span>City</span><input value={newTeamCity} onChange={(event) => setNewTeamCity(event.target.value)} placeholder="City" /></label><label className="field"><span>Team logo / crest URL</span><input type="url" maxLength={2000} value={badgeUrl} onChange={(event) => setBadgeUrl(event.target.value)} placeholder="https://…/team-logo.png" /></label><label className="field"><span>Banner image URL</span><input type="url" maxLength={2000} value={bannerUrl} onChange={(event) => setBannerUrl(event.target.value)} placeholder="https://…/team-banner.jpg" /></label><button className="button" disabled={creating}>{creating ? "Creating…" : "Create Team"}</button></form> : <div className="state-card"><strong>Team creation needs a Community.</strong><p className="muted">You must be a Community Founder or Coach before creating its Team.</p></div> : <div className="member-gate"><strong>Want to create or challenge with a Team?</strong><span className="muted">Public discovery stays open. Authentication is required only for member actions.</span>{signInHref ? <a className="button secondary" href={signInHref}>Sign in</a> : <span className="muted">Open HOOMA through Telegram to authenticate.</span>}</div>}{loading && !teams.length ? <div className="state-card"><strong>Loading Teams…</strong></div> : null}{!loading && !teams.length && !error ? <div className="state-card"><strong>No Teams found.</strong><p className="muted">Try a different search, city or houma.</p></div> : null}{teams.length ? <div className="team-discovery-grid">{teams.map((team) => <TeamDiscoveryCard key={team.id} id={team.id} name={team.name} badgeUrl={team.badgeUrl} bannerUrl={team.bannerUrl} city={team.city} houma={team.houma} motto={team.motto} playerCount={team._count.players} challengeHref={me && managedTeams.some((managed) => managed.id !== team.id) ? "/teams/control" : null} />)}</div> : null}{nextCursor ? <button className="button secondary teams-load-more" type="button" disabled={loading} onClick={() => void loadMore()}>{loading ? "Loading…" : "Load more Teams"}</button> : null}</> : null}{tab === "challenges" ? <section className="panel"><span className="eyebrow">COACH CONTROL ROOM</span><div className="teams-section-heading"><div><h2>Team challenges</h2><p className="muted">Incoming and outgoing requests for Teams you are authorized to manage.</p></div><a className="button secondary" href="/teams/control">Open Control Room</a></div>{memberError ? <div className="error-box">{memberError}</div> : null}{memberLoading ? <p className="muted">Loading challenges…</p> : null}{!memberLoading && !challenges.length ? <p className="muted">No Team challenges yet.</p> : null}<div className="challenge-list">{challenges.map((challenge) => { const incomingChallenge = managedIds.has(challenge.challengedTeamId); const outgoingChallenge = managedIds.has(challenge.challengerTeamId); return <article className="challenge-card" key={challenge.id}><div><span className="eyebrow">{incomingChallenge ? "INCOMING" : "OUTGOING"} · {challenge.status}</span><h3>{challenge.challengerTeam.name} vs {challenge.challengedTeam.name}</h3><p>{challenge.format.replaceAll("_", " ")} · {friendlyDate(challenge.proposedAt)}</p>{challenge.message ? <p className="muted">{challenge.message}</p> : null}</div>{challenge.status === "PENDING" ? <div className="challenge-actions">{incomingChallenge ? <><button className="button" type="button" disabled={actingChallengeId === challenge.id} onClick={() => void respondToChallenge(challenge.id, "accept")}>Accept</button><button className="button secondary" type="button" disabled={actingChallengeId === challenge.id} onClick={() => void respondToChallenge(challenge.id, "decline")}>Decline</button></> : null}{outgoingChallenge ? <button className="button secondary" type="button" disabled={actingChallengeId === challenge.id} onClick={() => void respondToChallenge(challenge.id, "cancel")}>Cancel</button> : null}</div> : null}</article>; })}</div></section> : null}{tab === "games" ? <section className="panel"><span className="eyebrow">MATCHDAY</span><div className="teams-section-heading"><div><h2>Accepted games</h2><p className="muted">Accepted challenges become Team games without inventing missing schedule details.</p></div><a className="button secondary" href="/teams/control">Open Control Room</a></div>{memberError ? <div className="error-box">{memberError}</div> : null}{memberLoading ? <p className="muted">Loading games…</p> : null}{!memberLoading && !games.length ? <p className="muted">No accepted Team games yet.</p> : null}<div className="game-list">{games.map((game) => <article className="team-game-card" key={game.id}><div><span className="eyebrow">{game.status}</span><h3>{game.homeTeam.name} <em>vs</em> {game.awayTeam.name}</h3><p>{friendlyDate(game.scheduledAt)}</p></div><strong>{game.status === "SCHEDULING" ? "TBA" : "MATCH"}</strong></article>)}</div></section> : null}</div>;
}
