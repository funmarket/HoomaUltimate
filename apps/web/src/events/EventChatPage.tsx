import { FormEvent, useEffect, useState } from "react";
import { eventApi, type EventChatRecord } from "../api/event-client";
export function EventChatPage({ eventId }: { eventId: string }) { const [messages, setMessages] = useState<EventChatRecord[]>([]); const [error, setError] = useState(""); const reload = () => eventApi.chat(eventId).then(setMessages).catch((reason: Error) => setError(reason.message)); useEffect(() => { void reload(); }, [eventId]); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const data = new FormData(form); void eventApi.postChat(eventId, String(data.get("body"))).then(() => { form.reset(); return reload(); }).catch((reason: Error) => setError(reason.message)); } return <section><p className="eyebrow">TEMPORARY EVENT CHAT</p><h2>Match chat</h2><p className="muted">Available only to active participants during the event window. Messages expire with the room.</p>{error ? <p className="error">{error}</p> : null}<div className="chat-list">{messages.map((message) => <article key={message.id}><strong>{message.user?.presentation?.displayName ?? "Player"}</strong><p>{message.body}</p></article>)}</div><form className="chat-compose" onSubmit={submit}><input name="body" maxLength={1200} placeholder="Message the players" required /><button type="submit">Send</button></form></section>; }
